import { NgModule } from '@angular/core';
import { PoolComponent } from './pool.component';
import { routing } from './pool.routing';

@NgModule({
  imports: [routing],
  declarations: [PoolComponent]
})
export class PoolModule { }
